name: internal_benefits_library
description: A new Flutter plugin project.
version: 0.0.1
homepage:

environment:
  sdk: ">=2.17.6 <3.0.0"
  flutter: ">=2.5.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_web_plugins:
    sdk: flutter
  get_it: ^7.2.0
  package_info_plus: ^1.4.3+1
  plugin_platform_interface: ^2.0.2
  retrofit: ^3.0.1+1
  shared_preferences: ^2.0.15
  url_launcher: ^6.1.5
  json_serializable: ^6.3.1
  flutter_bloc: ^8.1.1
  equatable: ^2.0.5
  firebase_analytics: ^9.3.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  retrofit_generator: any
  build_runner: any

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  # This section identifies this Flutter project as a plugin project.
  # The 'pluginClass' specifies the class (in Java, Kotlin, Swift, Objective-C, etc.)
  # which should be registered in the plugin registry. This is required for
  # using method channels.
  # The Android 'package' specifies package in which the registered class is.
  # This is required for using method channels on Android.
  # The 'ffiPlugin' specifies that native code should be built and bundled.
  # This is required for using `dart:ffi`.
  # All these are used by the tooling to maintain consistency when
  # adding or updating assets for this project.
  plugin:
    platforms:
      android:
        package: com.bamba.internal.internal_benefits_library
        pluginClass: InternalBenefitsLibraryPlugin
      ios:
        pluginClass: InternalBenefitsLibraryPlugin
      web:
        pluginClass: InternalBenefitsLibraryWeb
        fileName: internal_benefits_library_web.dart
  assets:
    - assets/
    - assets/icons/
    - assets/images/
